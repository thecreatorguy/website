version: '3.8'

services:
  website:
    environment:
      HTTPS: "1"
      SSL_CERT_PATH: /certs/fullchain.pem
      SSL_KEYFILE_PATH: /certs/privkey.pem
    volumes:
      - "/etc/letsencrypt/live/itstimjohnson.com:/certs"
